enum ConstantMarkerIds {
  currentLocationMarker,
}

enum AreaSafety {
  safe,
  warning,
  danger,
}

enum ParkingSlotType {
  parallel,
  angled,
  perpendicular,
}

enum ParkingAreaType {
  onStreet,
  offStreet,
  garageParking,
}
